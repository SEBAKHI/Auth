# Set this to the deployed Auth API origin at build time.
# Must match the CSP connect-src in public/web.config.
VITE_API_BASE_URL=https://auth-sandbox.sebakhi.com
# End-user self-service app (profile, invitations).
VITE_ACCOUNTS_URL=https://accounts-sandbox.sebakhi.com
