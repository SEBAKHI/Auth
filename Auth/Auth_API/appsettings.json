{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_dependencies_comment": "=== CONNECTION STRINGS ===",
  "ConnectionStrings": {
    "_comment": "Development: Uses Windows Auth. Production: Use appsettings.Production.json with SQL Auth + Encrypt=True",
    "AuthDb": "ConnectionStrings__AuthDb"
  },

  "_jwt_comment": "=== JWT AUTHENTICATION ===",
  "Jwt": {
    "_comment": "Keys are auto-generated on first startup via DPAPI. Override Issuer/Audience for production.",
    "Issuer": "{{JWT_ISSUER_URL}}",
    "Audience": "{{JWT_AUDIENCE_URL}}",
    "AccessTokenLifetimeMinutes": 15,
    "RefreshTokenLifetimeDays": 7,
    "KeyId": "auth-key-1",
    "RotateRefreshTokens": true,
    "ClockSkewSeconds": 60,
    "PrivateKeyPath": "",
    "PrivateKeyPem": "",
    "PrivateKeyEncrypted": "",
    "RefreshTokenEncryptedKey": ""
  },

  "_security_comment": "=== SECURITY CONFIGURATION ===",
  "DataProtection": {
    "_comment": "KeyPath: where the Data Protection key ring is stored. Empty = default %ProgramData%\\AuthSystem\\Keys. On IIS / shared hosting the app-pool identity often cannot write under %ProgramData%, so set this to a writable folder OUTSIDE the public web root (the SAME folder the API Gateway uses, so both share one ring).",
    "KeyPath": "",
    "Certificate": {
      "_comment": "Only used when SecretManagement:StorageMode = Certificate. Prefer PasswordEnvironmentVariable over storing Password here.",
      "PfxPath": "",
      "Password": "",
      "PasswordEnvironmentVariable": "AUTH_DP_CERT_PASSWORD",
      "Thumbprint": "",
      "AdditionalPfxPaths": []
    }
  },
  "SecretManagement": {
    "_comment": "StorageMode: PlainText | Certificate (default) | Dpapi. Certificate needs DataProtection:Certificate below; in Development ONLY, a missing certificate falls back to PlainText so a fresh clone runs without setup — every other environment fails to start instead. Keys auto-generate on first startup; disable AutoGenerateKeys after initial setup in production.",
    "StorageMode": "Certificate",
    "SecretFilePath": "",
    "_PlainTextTargetFile_comment": "Where PlainText-mode secrets are written. Empty = the running environment's own appsettings.{Environment}.json, which is what you want; set a path only to override.",
    "PlainTextTargetFile": "",
    "AutoGenerateKeys": true,
    "EnableAdminApi": true,
    "RequiredPermission": "secrets.manage"
  },

  "_healthchecks_comment": "=== HEALTH CHECKS ===",
  "HealthChecks": {
    "_comment": "ExposeErrorDetails: include failed-check exception messages in the /health and /ready JSON. /health and /ready are PUBLIC (exempt from gateway-token validation), so keep this false in production; enable briefly only to diagnose. Full errors are always in the server logs.",
    "ExposeErrorDetails": false
  },

  "_password_comment": "=== PASSWORD POLICY (Argon2id - OWASP Recommended) ===",
  "Password": {
    "MinimumLength": 8,
    "RequireUppercase": true,
    "RequireLowercase": true,
    "RequireDigit": true,
    "RequireSpecialCharacter": true,
    "HistoryCount": 3,
    "MaxFailedAttempts": 5,
    "LockoutDurationMinutes": 15,
    "Argon2MemorySize": 19456,
    "Argon2Iterations": 2,
    "Argon2Parallelism": 1,
    "SaltSize": 16,
    "HashSize": 32,
    "_pepper_comment": "Server-side secret mixed into every hash (defense-in-depth vs DB-only breach). Only 'Enabled' is set here; CurrentKeyId/Keys are secret-managed (auto-provisioned on startup when enabled). WARNING: once enabled, the pepper must be backed up like the JWT keys - losing it locks out all users.",
    "Pepper": {
      "Enabled": false
    },
    "_breach_comment": "Reject or warn on known-breached passwords via the free, keyless HIBP Pwned Passwords range API (k-anonymity). Mode: Enforce = reject; Warn = allow but return X-Password-Warning header. FailOpen = allow if HIBP is unreachable. Fully inert (no external call) when Enabled=false.",
    "BreachedPasswordCheck": {
      "Enabled": false,
      "Mode": "Enforce",
      "FailOpen": true,
      "RejectThreshold": 1,
      "TimeoutMs": 2000
    }
  },

  "_gateway_comment": "=== API GATEWAY PROTECTION ===",
  "Gateway": {
    "_comment": "ExpectedToken is auto-generated and loaded from DPAPI secrets file on first startup",
    "TokenHeaderName": "X-Gateway-Token",
    "ValidationEnabled": true,
    "ExemptPaths": [
      "/.well-known/",
      "/health",
      "/ready",
      "/swagger",
      "/openapi",
      "/uploads/"
    ]
  },

  "_session_comment": "=== SESSION MANAGEMENT ===",
  "Session": {
    "LifetimeHours": 24,
    "ExtendOnActivity": true,
    "ExtensionHours": 1,
    "MaxConcurrentSessions": 5,
    "TerminateOldestOnMax": true,
    "IdleTimeoutMinutes": 30
  },

  "_imagestorage_comment": "=== IMAGE STORAGE (profile pictures, org/app logos) ===",
  "ImageStorage": {
    "_comment": "Filesystem provider; the DB stores a relative key and the API composes PublicBaseUrl+key at read time. Swap Provider for object storage later without code changes. PublicBaseUrl/PhysicalPath are overridden per environment.",
    "Provider": "filesystem",
    "PhysicalPath": "App_Data/uploads/images",
    "PublicBaseUrl": "/uploads/images",
    "RequestPath": "/uploads/images",
    "MaxSizeBytes": 4194304,
    "MaxMegapixels": 50,
    "MaxEdgePx": 1024,
    "WebpQuality": 90,
    "AllowedContentTypes": [ "image/png", "image/jpeg", "image/webp", "image/gif" ]
  },

  "_notifications_comment": "=== NOTIFICATION DISPATCH (DB-managed templates; UseOutbox=true queues sends for the background dispatcher) ===",
  "Notifications": {
    "UseOutbox": false,
    "PollIntervalSeconds": 30,
    "BatchSize": 20,
    "MaxAttempts": 5,
    "StaleClaimMinutes": 5
  },

  "_accountdeletion_comment": "=== ACCOUNT DELETION (two-phase: 30-day recoverable grace, then staged irreversible destruction) === IdentifierHmacKeyPlain is NEVER set here: PlainText mode auto-generates it into the git-ignored local settings layer; Certificate/Dpapi mode provisions AccountDeletionIdentifierHmacKey in the encrypted secrets file. That key is PERMANENT (identifier reservations depend on it) - never rotate it. PolicyVersion format is YYYY.MM; bump it when the documented retention policy changes.",
  "AccountDeletion": {
    "GraceDays": 30,
    "WorkerPollMinutes": 15,
    "MaxExecutionAttempts": 5,
    "OtpExpirationMinutes": 15,
    "LoginAttemptRetentionDays": 365,
    "OutboxRetentionDays": 180,
    "PolicyVersion": "2026.07",
    "_migration_comment": "RunEncryptionMigration: enable for ONE deployment to re-encrypt TOTP secrets and phone numbers under per-user keys (idempotent; watch the logged report), then disable.",
    "RunEncryptionMigration": false
  },

  "_email_comment": "=== EMAIL SERVICE ===",
  "Email": {
    "_comment": "Configure SMTP for password reset and email verification. Set Enabled=true when ready.",
    "SmtpHost": "{{SMTP_HOST}}",
    "SmtpPort": 587,
    "UseSsl": true,
    "Username": "{{SMTP_USERNAME}}",
    "Password": "Email__Password",
    "SenderEmail": "{{SENDER_EMAIL}}",
    "SenderName": "Auth System",
    "FrontendBaseUrl": "{{FRONTEND_BASE_URL}}",
    "OtpExpirationMinutes": 5,
    "ResetTokenExpirationMinutes": 30,
    "RateLimitWindowSeconds": 60,
    "MaxOtpRequestsPerWindow": 3,
    "Enabled": false
  },

  "_identityprovider_comment": "=== IDENTITY PROVIDER (authorization-code + PKCE, IdP session cookie) ===",
  "IdentityProvider": {
    "_comment": "AccountsBaseUrl = public origin of the accounts SPA (authorize redirects unauthenticated users to its /login). PublicBaseUrl = this API's OWN public origin (e.g. https://auth.example.com) — REQUIRED behind a reverse proxy, since Request.Host is the internal destination; used to build the authorize returnTo and the discovery-document endpoints.",
    "AccountsBaseUrl": "{{ACCOUNTS_BASE_URL}}",
    "PublicBaseUrl": "{{AUTH_PUBLIC_BASE_URL}}",
    "AuthorizationCodeLifetimeSeconds": 60,
    "IdpSessionCookieName": "auth_idp",
    "IdpSessionLifetimeDays": 7
  },

  "_externalauth_comment": "=== EXTERNAL AUTHENTICATION PROVIDERS ===",
  "ExternalAuth": {
    "Google": {
      "Enabled": true,
      "ClientId": "{{GOOGLE_CLIENT_ID}}"
    },
    "_apple_comment": "Enable only after the Apple Developer prerequisites exist (Services ID, verified domain + return URLs, .p8 key). PrivateKeyPem is NEVER set here: provision AppleSigningKeyPem in the encrypted secrets file (Certificate/Dpapi) or the git-ignored local settings layer in Development. Also flip the seeded 'apple' row in ExternalAuthProviders to IsEnabled=1.",
    "Apple": {
      "Enabled": false,
      "ServicesId": "{{APPLE_SERVICES_ID}}",
      "TeamId": "{{APPLE_TEAM_ID}}",
      "KeyId": "{{APPLE_KEY_ID}}"
    }
  },

  "_cors_comment": "=== CORS CONFIGURATION (OWASP A02) ===",
  "Cors": {
    "_comment": "REQUIRED for production: Specify allowed origins. Empty array + non-dev environment = startup error",
    "AllowedOrigins": [
      "{{FRONTEND_ORIGIN_1}}",
      "{{FRONTEND_ORIGIN_2}}"
    ],
    "AllowCredentials": true
  },

  "_ratelimit_comment": "=== RATE LIMITING ===",
  "RateLimiting": {
    "_comment": "One entry per NAMED policy that endpoints actually opt into (see Program.cs). There is no general/default bucket by design.",
    "LoginPermitLimit": 20,
    "LoginWindowSeconds": 60,
    "PasswordResetPermitLimit": 10,
    "PasswordResetWindowSeconds": 60
  },

  "_logging_comment": "=== STRUCTURED LOGGING (Serilog) ===",
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information",
        "System": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj}{NewLine}{Exception}"
        }
      },
      {
        "Name": "File",
        "Args": {
          "path": "Logs/auth-api-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 30
        }
      }
    ],
    "Enrich": [ "FromLogContext", "WithMachineName", "WithThreadId" ]
  }
}
